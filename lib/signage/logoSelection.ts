/**
 * Client-only sentinel for pack/ad-hoc logo pickers. Serialized to JSON `null` so the generate API
 * skips logos even when a venue profile sets defaults.
 */
export const SIGNAGE_LOGO_NONE_SENTINEL = "__signage_explicit_no_logo__";
